# 简历

下面这版可以直接放到简历里。项目名建议不要写“muduo复现”，而写成“C++11 Reactor 高并发网络库”，更像一个独立项目。

**C++11 Reactor 高并发网络库**

**项目描述：**
参考 muduo 网络库核心设计，基于 C++11 实现了一个事件驱动 TCP 网络库，封装了 Reactor 模型、非阻塞 IO、epoll 事件分发、TCP 连接管理、应用层缓冲区、定时器和多线程 EventLoop 等核心模块，可用于构建高并发 TCP 服务端和客户端程序。

**技术栈：**
C++11、Linux、Socket、epoll、eventfd、timerfd、CMake、Reactor 模型、多线程编程、智能指针

**主要工作：**

1. 设计并实现 `EventLoop`、`Channel`、`Poller`、`EPollPoller` 等核心模块，基于 epoll 完成事件监听与分发，将 socket 读写事件、timerfd 定时事件和 eventfd 唤醒事件统一纳入事件循环处理。
2. 实现 `TcpServer`、`Acceptor`、`TcpConnection` 等服务端核心组件，支持新连接接收、连接建立/关闭回调、读写事件处理、发送完成回调和优雅关闭流程。
3. 设计 `Buffer` 应用层缓冲区，解决 TCP 字节流场景下读写不完整、非阻塞写不完、发送数据堆积等问题，并支持高水位回调用于连接限流和慢客户端处理。
4. 实现 `EventLoopThread` 和 `EventLoopThreadPool`，支持 one loop per thread 的多 Reactor 多线程模型，实现主 Reactor 负责接收连接、子 Reactor 负责连接 IO 处理的并发结构。
5. 基于 `timerfd` 实现 `Timer`、`TimerId`、`TimerQueue` 定时器模块，支持 `runAfter`、`runEvery` 等延迟任务和周期任务，可用于心跳检测、连接超时和定时资源清理。
6. 实现 `TcpClient` 与 `Connector`，支持非阻塞 connect 流程，并通过 `getsockopt(SO_ERROR)` 判断异步连接结果，完成客户端连接建立与异常处理。

**项目亮点：**

- 采用 Reactor 事件驱动模型，避免传统一连接一线程模型带来的资源浪费。
- 使用 `shared_ptr` / `weak_ptr` 管理 `TcpConnection` 生命周期，避免连接关闭过程中的悬空回调问题。
- 使用 `eventfd` 实现跨线程唤醒，使其他线程可以安全地向 IO 线程投递任务。
- 使用 `timerfd` 将定时器事件接入 epoll，实现定时任务与网络事件的统一调度。
- 封装 TCP 连接生命周期，支持连接建立、消息到达、发送完成、连接关闭等完整回调机制。

如果你的简历篇幅比较紧，可以用下面这个压缩版：

**C++11 Reactor 高并发网络库**
参考 muduo 网络库核心设计，基于 C++11 实现事件驱动 TCP 网络库，封装 Reactor 模型、非阻塞 IO、epoll 事件分发、TCP 连接管理、Buffer 缓冲区、timerfd 定时器和多线程 EventLoop 等模块。项目支持 TCP 服务端/客户端、连接生命周期管理、读写回调、优雅关闭、发送完成回调和周期定时任务。

**技术栈：** C++11、Linux、Socket、epoll、eventfd、timerfd、CMake、多线程、Reactor 模型

**主要工作：**

- 实现 `EventLoop`、`Channel`、`Poller`、`EPollPoller`，基于 epoll 完成 IO 事件监听与回调分发。
- 实现 `TcpServer`、`Acceptor`、`TcpConnection`，支持连接建立、消息处理、连接关闭和优雅 shutdown。
- 设计 `Buffer` 缓冲区，处理 TCP 字节流读写不完整、非阻塞写不完和发送数据堆积问题。
- 实现 `EventLoopThreadPool`，支持 one loop per thread 多 Reactor 并发模型。
- 基于 `timerfd` 实现 `TimerQueue`，支持延迟任务和周期任务，可用于心跳检测与连接超时。
- 实现 `TcpClient` 和 `Connector`，支持非阻塞 connect 以及异步连接结果判断。

#### 相关技能

- 熟练掌握C++，运用封装、继承、多态实现高内聚低耦合模块设计；

-  熟练应用C++11智能指针、lambda表达式、移动语义等特性提升代码安全性与性能；

-  基于单例、工厂、观察者模式完成ROS2节点架构设计，支撑多模块松耦合通信；

-  深入理解数组、链表、栈、队列、二叉树及STL vector/map/unordered_map等容器的底层实现与适用场景；

-  开发ROS2节点，通过Topic发布/订阅实时传感器数据，利用Service实现跨节点同步控制，借助rqt_graph、ros2 topic echo等工具完成系统调试；

-  掌握Linux内存管理（页式分配、虚拟地址映射）、进程调度与IPC机制（共享内存、消息队列、Socket）；

-  完成Linux平台下文件IO、多线程网络编程（epoll+Reactor模型）及系统级应用开发；

-  熟练使用Makefile构建项目、GDB定位段错误与死锁、Git进行分支协同与版本管理。



**高性能泛型线程池（支持优先级调度与并行任务模型）**
**技术栈：C++11、STL、Linux、多线程、模板元编程、RAII**

• 基于 C++11 多线程组件实现线程池核心调度框架，支持任务提交、线程唤醒、并发执行与安全关闭。
• 设计 `priority + order` 双关键字优先级调度机制，实现高优先级任务优先执行，同时保证同优先级任务 FIFO 稳定有序。
• 封装泛型任务提交模型，结合模板类型推导、`std::promise/std::future` 实现任意可调用对象的异步执行、返回值获取与异常传递。
• 实现 `multi_future` 与 `bulk / block / loop / sequence` 等并行任务模型，支持批量任务管理、区间分块和并行循环，提升线程池的并行计算能力。
• 基于 `thread_local + RAII` 构建 worker 上下文管理与内部等待死锁检测机制，并支持 `pause / resume / purge / reset / shutdown` 等运行期控制能力。





**高性能泛型线程池（支持优先级调度与并行任务模型）**
**2025年12月 - 2026年03月**
**技术栈：C++11、STL、Linux、多线程、模板元编程、RAII**

• 基于 `std::thread`、`std::mutex`、`std::condition_variable` 实现线程池核心调度模型，完成任务提交、线程唤醒、任务执行与线程安全退出等基础能力。
• 针对普通 FIFO 队列无法区分任务紧急程度的问题，基于 `priority_queue` 设计 `priority + order` 双关键字任务调度机制，实现高优先级任务优先执行，并保证同优先级任务 FIFO 顺序。
• 针对线程池需要支持不同类型任务的问题，封装泛型任务模型，结合 `std::result_of`、`std::decay` 和模板类型推导，实现任意可调用对象的统一提交与返回值类型自动推导。
• 基于 `std::promise/std::future` 构建异步任务返回机制，支持带返回值任务提交，并将 worker 线程中的异常安全传递给调用方，提升任务执行的可靠性。
• 实现 `multi_future` 批量任务结果管理机制，支持多任务统一 `wait / get / 状态检测`，降低批量异步任务的管理复杂度。
• 在基础任务提交能力上扩展 `bulk / block / loop / sequence` 等并行任务模型，支持批量任务、区间分块、并行循环和顺序任务分发，提升线程池在并行计算场景下的复用能力。
• 基于 `thread_local` 与 RAII 设计 worker 线程上下文管理机制，实现 worker 身份识别，并在内部等待任务时进行死锁检测，避免线程池内任务相互等待导致阻塞。
• 支持 `pause / resume / purge / reset / shutdown` 等运行期控制接口，并优化 `notify_one / notify_all` 唤醒策略，减少无效唤醒和线程竞争开销。



# 面试问题

如果我是 **字节跳动 / 小红书 / 美团** 的 C++ 后端面试官，看到你简历上写了这个项目：

> **C++11 Reactor 高并发网络库**

我不会只问“你做了什么”，我会顺着你的项目模块一层一层追问。下面这些就是很可能被问到的问题。

------

# 一、项目整体设计类问题

### 1. 你这个项目是做什么的？

面试官可能问：

> 你这个 C++11 Reactor 网络库主要解决什么问题？
> 和直接用 socket 编程相比，它封装了什么？

你要能答：

> 这个项目是对 muduo 核心思想的复现，封装了基于 epoll 的 Reactor 网络模型。用户不需要直接操作 socket、epoll、fd 事件，只需要设置连接回调、消息回调、发送回调，就可以实现 TCP 服务端或客户端。

------

### 2. 为什么要做这个项目？

可能会问：

> 你为什么选择复现 muduo？
> 这个项目对你最大的提升是什么？

你可以答：

> 我之前学习过 Linux socket 和 C++，但直接写业务服务器会发现 socket、epoll、非阻塞 IO、连接生命周期管理非常复杂，所以通过复现 muduo 来理解高性能网络库底层设计。这个项目让我掌握了 Reactor 模型、one loop per thread、Buffer、timerfd、eventfd、TcpConnection 生命周期管理等核心内容。

------

### 3. 你这个项目有哪些核心模块？

可能会问：

> 你给我讲一下你的项目结构。

你要按层次讲：

```text
EventLoop：事件循环核心
Channel：fd 和事件回调的封装
Poller / EPollPoller：epoll 的封装
Acceptor：监听新连接
TcpConnection：管理一条 TCP 连接
TcpServer：服务端封装
TcpClient / Connector：客户端连接封装
Buffer：应用层缓冲区
TimerQueue：定时器队列
EventLoopThreadPool：多线程 Reactor
```

------

# 二、Reactor 模型相关问题

### 4. 什么是 Reactor 模型？

面试官可能问：

> 你项目里说用了 Reactor 模型，Reactor 模型到底是什么？

你要答：

> Reactor 模型是一种事件驱动模型。应用程序把 fd 关心的事件注册到事件分发器中，比如 epoll。当 fd 可读、可写或出错时，事件循环被唤醒，然后根据事件类型调用对应回调。它的核心是“事件监听”和“事件处理”分离。

------

### 5. 你的项目里 Reactor 模型是怎么落地的？

你可以这样答：

```text
EventLoop 负责循环等待事件；
Poller/EPollPoller 负责调用 epoll_wait；
Channel 负责保存 fd 和对应回调；
TcpConnection/Acceptor/TimerQueue 通过 Channel 把事件注册到 EventLoop；
当 epoll 返回事件后，EventLoop 调用 Channel::handleEvent() 分发到具体回调。
```

------

### 6. Reactor 和传统一个连接一个线程有什么区别？

可能会问：

> 为什么不用一个连接一个线程？

你答：

> 一个连接一个线程在连接数较多时线程数量会急剧增加，线程切换成本高，内存开销大。Reactor 模型可以用少量 IO 线程管理大量连接，通过 epoll 监听多个 fd 的事件，只有 fd 真正就绪时才处理，从而提高并发能力。

------

# 三、EventLoop 相关问题

### 7. EventLoop 是什么？

面试官可能问：

> 你项目里的 EventLoop 是干什么的？

你答：

> EventLoop 是事件循环对象，是整个网络库的核心。它负责调用 epoll_wait 等待事件，然后分发给对应 Channel，同时还处理跨线程投递的任务和定时器任务。muduo 的思想是 one loop per thread，一个线程最多拥有一个 EventLoop。

------

### 8. 为什么一个线程只能有一个 EventLoop？

可能追问：

> one loop per thread 为什么要这么设计？

你答：

> 因为 EventLoop 管理的是该线程内的 fd 事件和任务队列，如果一个线程有多个 EventLoop，就会出现 fd 归属混乱、任务调度混乱的问题。一个线程一个 EventLoop 可以明确线程归属，所有 Channel 的 update/remove 都在自己的 EventLoop 线程中执行，减少锁的使用。

------

### 9. EventLoop 为什么要有 `assertInLoopThread()`？

可能问：

> 你代码里很多地方有 `loop_->assertInLoopThread()`，为什么？

你答：

> 因为 Channel、Poller、TcpConnection 的很多操作不是线程安全的，比如更新 epoll 事件、删除 Channel、修改连接状态等，都应该在所属 IO 线程执行。`assertInLoopThread()` 用来检查当前操作是否发生在正确的 EventLoop 线程中。

------

### 10. 如果其他线程想让 EventLoop 执行一个任务怎么办？

可能问：

> 其他线程调用 EventLoop 里的函数安全吗？

你答：

> 不能直接操作 EventLoop 内部结构。其他线程应该通过 `runInLoop()` 或 `queueInLoop()` 把任务投递到 EventLoop 的任务队列中，然后通过 `eventfd` 唤醒 IO 线程，让 EventLoop 在自己的线程里执行这些任务。

------

# 四、Channel 相关问题

### 11. Channel 是什么？

可能问：

> Channel 在你的项目里起什么作用？

你答：

> Channel 是 fd 和事件回调的封装。它不拥有 fd，只保存 fd 关注的事件、epoll 返回的实际事件，以及读、写、关闭、错误回调。EventLoop 得到活跃事件后，会调用 Channel::handleEvent()，再分发到具体回调。

------

### 12. Channel 为什么不拥有 fd？

可能追问：

> 既然 Channel 保存 fd，为什么不负责关闭 fd？

你答：

> Channel 只是事件分发器，不负责资源所有权。fd 的生命周期一般由 Socket 或 TcpConnection 管理。这样职责更清晰：Socket 负责 fd 的 RAII，Channel 只负责事件和回调。

------

### 13. Channel 的 `tie()` 是干什么的？

这很高频。

可能问：

> 你代码里 `channel_->tie(shared_from_this())` 是做什么的？

你答：

> `tie()` 用 weak_ptr 绑定 TcpConnection，防止 Channel 处理事件时 TcpConnection 已经被析构。事件发生时，Channel 先把 weak_ptr 提升成 shared_ptr，如果提升成功，说明连接还活着，再执行回调；如果失败，就不执行，避免悬空指针。

------

# 五、Poller / EPollPoller 相关问题

### 14. Poller 和 EPollPoller 是什么关系？

你答：

> Poller 是抽象基类，定义 updateChannel、removeChannel、poll 等接口。EPollPoller 是基于 Linux epoll 的具体实现。这样可以把 EventLoop 和具体 IO 多路复用机制解耦，理论上可以替换成 poll 或 select 实现。

------

### 15. epoll 的 LT 和 ET 区别是什么？你用的是哪种？

你可以答：

> LT 是水平触发，只要 fd 还有数据可读或还能写，epoll_wait 就会一直返回。ET 是边缘触发，只有状态从不可读变成可读时才通知一次。我的项目更接近 muduo，默认使用 LT，逻辑更简单，也不容易漏读。

如果被追问 ET 要注意什么：

> ET 模式下必须配合非阻塞 IO，并且 read/write 要循环读到 EAGAIN，否则可能漏事件。

------

### 16. 为什么 socket 要设置非阻塞？

你答：

> Reactor 线程不能被某个 fd 阻塞。如果 socket 是阻塞的，read/write/connect 都可能卡住整个 EventLoop，导致其他连接无法处理。所以所有 socket 都应该设置为 non-blocking。

------

# 六、TcpConnection 相关问题

### 17. TcpConnection 表示什么？

你答：

> TcpConnection 表示一条已经建立的 TCP 连接，负责管理连接状态、读写事件、输入输出 Buffer、连接关闭、错误处理和各种用户回调。

------

### 18. TcpConnection 的生命周期怎么管理？

高频。

你答：

> TcpConnection 用 shared_ptr 管理。TcpServer 的 connections_ map 持有连接对象，Channel 通过 tie 保存 weak_ptr。连接关闭时，handleClose 触发 closeCallback，TcpServer 从 map 中移除连接，最后在 loop 线程中调用 connectDestroyed 清理 Channel。这样可以保证事件处理过程中对象不会提前析构。

------

### 19. 为什么 TcpConnection 继承 `enable_shared_from_this`？

可能问：

> 为什么要用 `shared_from_this()`？

你答：

> 因为很多操作是异步投递到 EventLoop 的，比如 send、shutdown、writeCompleteCallback。如果只捕获 this，可能任务执行前 TcpConnection 已经析构。用 shared_from_this() 可以在 lambda 中持有 shared_ptr，延长对象生命周期，保证任务执行期间对象有效。

------

### 20. `send()` 为什么要区分是否在 loop 线程？

可能问：

> TcpConnection::send 为什么要判断 `loop_->isInLoopThread()`？

你答：

> 如果当前就在连接所属的 IO 线程，可以直接调用 sendInLoop；如果不在，就不能直接操作 outputBuffer 和 Channel，需要通过 runInLoop 投递到 IO 线程执行，保证线程安全。

------

### 21. 为什么 send 里要拷贝 message？

你之前问过这个，很容易被问。

你答：

> 因为 send 可能跨线程投递，真正的 sendInLoop 不是马上执行。如果直接引用外部 message，send 返回后 message 可能销毁。拷贝一份到 lambda 里可以保证异步执行时数据还存在。

------

### 22. `shutdown()` 为什么不是直接 close？

你答：

> shutdown 是优雅关闭，只关闭写端，允许对方继续读取已经发送的数据。TcpConnection 在调用 shutdown 时，如果 outputBuffer 里还有未发送数据，会等数据发送完成后再 shutdownInLoop，避免数据丢失。直接 close 可能导致还没写完的数据被丢弃。

------

# 七、Buffer 相关问题

### 23. 为什么需要 Buffer？

高频。

你答：

> TCP 是字节流，没有消息边界，且非阻塞 IO 下 read/write 可能一次读不完或写不完。所以需要应用层 Buffer。inputBuffer 保存已读但业务还没处理的数据，outputBuffer 保存还没写入内核发送缓冲区的数据。

------

### 24. Buffer 怎么解决粘包半包？

你答：

> Buffer 本身不直接知道业务协议，但它提供连续读取和 retrieve 能力。上层 codec 可以从 inputBuffer 中判断是否收到了完整消息，比如先读固定长度头部，再根据长度字段判断 body 是否完整。完整就取出，不完整就继续留在 Buffer 中等待下一次 read。

------

### 25. outputBuffer 什么时候使用？

你答：

> sendInLoop 会先尝试直接 write 到 socket。如果内核发送缓冲区满了，或者只写出一部分，剩余数据就追加到 outputBuffer，并开启 Channel 的写事件。等 epoll 通知可写时，handleWrite 继续把 outputBuffer 中的数据写出去。

------

### 26. highWaterMarkCallback 是干什么的？

你答：

> 当 outputBuffer 堆积数据超过 highWaterMark_ 时触发 highWaterMarkCallback。它可以用于限流、断开慢客户端、记录日志，防止某个连接发送太慢导致服务器内存不断增长。

------

# 八、TimerQueue 相关问题

### 27. 为什么网络库需要定时器？

你答：

> 网络库中很多功能依赖时间，比如连接超时、心跳检测、定期清理空闲连接、延迟重连、周期统计等，所以需要把定时器接入 EventLoop。

------

### 28. TimerQueue 怎么实现？

你答：

> TimerQueue 内部用 timerfd 创建定时器 fd，并通过 Channel 注册到 EventLoop。所有 Timer 按到期时间放入 std::set，timerfd 始终设置为最近一个 Timer 的到期时间。到期后 timerfd 可读，EventLoop 调用 TimerQueue::handleRead，取出所有到期 Timer 执行回调，重复定时器重新插入。

------

### 29. 为什么不用 sleep 实现定时器？

你答：

> sleep 会阻塞 EventLoop 线程，导致这段时间不能处理任何网络事件。timerfd 可以和 socket fd 一样被 epoll 监听，定时器事件和 IO 事件统一调度，不会阻塞事件循环。

------

### 30. `timerfd` 到期后为什么要 read？

你答：

> timerfd 到期后会变成可读，必须 read 一次清除可读状态，否则 epoll 会一直认为它可读，导致 EventLoop 不断触发 handleRead。read 出来的 uint64_t 表示从上次读取到现在到期了多少次。

------

# 九、TcpClient / Connector 相关问题

### 31. 非阻塞 connect 怎么判断成功？

高频。

你答：

> 非阻塞 connect 可能返回 EINPROGRESS，表示连接正在进行。之后监听该 socket 的可写事件。当 epoll 通知可写时，不能直接认为连接成功，必须通过 getsockopt(sockfd, SOL_SOCKET, SO_ERROR) 获取真实错误码。如果 err == 0 表示连接成功，否则表示连接失败。

------

### 32. Connector 和 TcpClient 分别负责什么？

你答：

> Connector 只负责连接建立阶段，包括创建非阻塞 socket、发起 connect、监听可写事件、判断连接结果。TcpClient 负责客户端整体管理，连接成功后通过回调创建 TcpConnection，之后由 TcpConnection 管理读写和生命周期。

------

# 十、多线程模型相关问题

### 33. EventLoopThreadPool 是什么？

你答：

> EventLoopThreadPool 是 IO 线程池。主 EventLoop 负责监听新连接，接受连接后通过轮询选择一个子 EventLoop，把新连接分配给子 Reactor。子 EventLoop 负责该连接后续读写事件。

------

### 34. 为什么主 Reactor 不直接处理所有连接？

你答：

> 如果所有连接读写都由主 EventLoop 处理，连接数和数据量大时容易成为瓶颈。多 Reactor 模型可以让多个 IO 线程并行处理不同连接，提高并发能力。

------

### 35. 多线程下如何保证 TcpConnection 线程安全？

你答：

> 每个 TcpConnection 归属于一个固定 EventLoop，连接相关的 fd、Channel、Buffer 操作都在所属 IO 线程执行。跨线程调用 send/shutdown 时，通过 runInLoop/queueInLoop 投递到正确线程执行，避免直接加锁操作复杂共享状态。

------

# 十一、错误处理类问题

### 36. handleError 里为什么用 `getsockopt(SO_ERROR)`？

你答：

> epoll 返回 EPOLLERR 只说明 fd 出错，但不告诉具体错误码。errno 也不一定是这个 socket 的错误，所以需要通过 getsockopt(SOL_SOCKET, SO_ERROR) 读取 socket 保存的真实错误原因。

------

### 37. 对端关闭连接时你的流程是什么？

你答：

> handleRead 中 read 返回 0 表示对端关闭连接，然后调用 handleClose。handleClose 会设置状态为 kDisconnected，disableAll 取消事件，触发 connectionCallback 通知用户连接断开，再触发 closeCallback 通知 TcpServer 移除连接。

------

# 十二、面试官可能继续深挖的问题

这些是偏中高级、字节/美团可能追问的。

### 38. 你的网络库支持多少连接？瓶颈在哪里？

你可以答：

> 理论连接数主要受 fd 限制、内存、epoll 性能、业务处理耗时影响。我的项目是学习型实现，没有做压测和生产级优化。瓶颈可能在单线程 EventLoop、Buffer 内存增长、业务回调阻塞 IO 线程等方面。

------

### 39. 如果业务回调很耗时怎么办？

你答：

> 不能在 IO 线程里执行耗时业务，否则会阻塞 EventLoop。可以把耗时任务投递到业务线程池，IO 线程只负责收发数据和事件分发。业务线程处理完后再通过 runInLoop 把发送操作投递回 IO 线程。

------

### 40. 你这个项目和原版 muduo 有哪些差距？

这个一定要诚实。

你答：

> 我的项目主要复现核心网络流程，和原版 muduo 相比还有差距，比如日志系统、线程安全细节、异常处理、性能优化、测试覆盖、更多协议支持、生产级内存管理等还不完善。目前更偏学习型网络库，重点是理解 Reactor、TcpConnection 生命周期、Buffer、timerfd 和 eventfd 等核心设计。

------

# 十三、如果我是面试官，我会连续这样问你

你可以模拟一下真实面试链路：

```text
1. 你先介绍一下这个网络库项目。
2. Reactor 模型是什么？你项目里怎么体现？
3. EventLoop、Channel、Poller 三者关系是什么？
4. Channel 为什么要 tie TcpConnection？
5. TcpConnection 生命周期怎么保证？
6. send 为什么要区分 loop 线程和非 loop 线程？
7. Buffer 为什么需要？怎么解决半包粘包？
8. 非阻塞 write 写不完怎么办？
9. TimerQueue 怎么接入 EventLoop？
10. 非阻塞 connect 怎么判断成功？
11. 多线程模型怎么设计？
12. 如果业务回调很慢，会有什么问题？
13. 你这个项目和 muduo 原版有什么差距？
```

这套问下来，如果你能答清楚，项目就基本能撑住。

------

# 十四、你最应该优先背熟的 10 个问题

按重要程度排序：

```text
1. Reactor 模型是什么？
2. EventLoop、Channel、Poller 的关系是什么？
3. TcpConnection 生命周期怎么管理？
4. Channel::tie 是干什么的？
5. Buffer 为什么需要？
6. 非阻塞 write 写不完怎么办？
7. send 为什么要 runInLoop？
8. TimerQueue 怎么通过 timerfd 接入 EventLoop？
9. 非阻塞 connect 怎么判断连接成功？
10. one loop per thread 是什么？
```

这 10 个是你这个项目最核心的面试题。